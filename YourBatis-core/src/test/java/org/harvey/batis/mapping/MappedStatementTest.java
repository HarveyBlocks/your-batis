package org.harvey.batis.mapping;

import junit.framework.TestCase;

import java.util.Arrays;

public class MappedStatementTest extends TestCase {
    public void testDelimited() {
        /*final String src = " ,a , c, v asda     ,  b aaa        ,a ,";
        String[] delimited = MappedStatement.Builder.delimited(
                src
        );
        System.out.println(Arrays.toString(delimited));
        System.out.println(Arrays.toString(src.split(",")));*/
    }

}